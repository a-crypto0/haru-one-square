# 코드리뷰 보고서

- 일시: 2026-08-27
- 범위: T029 PWA·Vercel 배포 변경과 모바일 레이아웃 수정
- 감사 회차: 1차 + 회부 항목 재확인
- 방식: 단일 세션 감사로 독립성에 제한이 있으며, 실행 검증은 별도 테스트 보고서로 분리했다.

## 한눈에 보기

- 명세 일치율: **100%** — R018·F010·T029 기준
- 구조 감사: 통과 6 / 남은 지적 0
- Critical 0건 / Important 0건
- 강점: GitHub→Vercel 설정이 저장소 안에 고정되어 있고, PWA 캐시는 같은 출처 정적 파일에만 제한된다.

## 명세 대조표

| 항목 | 판정 | 내용 |
|---|---|---|
| R018 | ✅ | 명령어 없는 GitHub 업로드 안내와 Vercel Import 경로 제공 |
| F010 | ✅ | standalone manifest, 설치 아이콘, 서비스 워커, 오프라인 셸 구현 |
| T029 | ✅ | 웹 export·390px 동선·오프라인 재실행 완료 조건 충족 |
| 성인형 디자인 | ✅ | 기존 세이지·파스텔 토큰, 기록표 심볼, 캐릭터·점수 없음 |

## 구조 감사

- `public/`, 아이콘, `vercel.json`, 빌드 스크립트를 `frontend-ui` v1.1.0에 배정했다.
- `frontend-ui`와 모듈 인덱스 버전이 1.1.0으로 일치한다.
- 서비스 워커는 same-origin GET 정적 파일만 처리하고 외부 Supabase 요청을 가로채지 않는다.
- SPA rewrite, 서비스 워커 재검증 헤더, 해시 번들 장기 캐시가 분리되어 있다.
- 클라이언트 비밀키·직접 HTML 주입·타입 우회가 없다.
- `package.json`과 `package-lock.json`의 웹 의존성·Node 엔진이 일치한다.

## 회부 후 해결

1. 서비스 워커 등록 Promise의 실패 처리가 없던 문제 → 오류를 안전하게 종료하도록 처리.
2. 로컬 보안 컨텍스트에서 자동 PWA 등록을 검증할 수 없던 문제 → localhost·127.0.0.1을 허용하고 개발용 번들은 캐시에서 제외.

## 기존 제품 한계

이번 배포 변경과 무관하게 실제 Supabase 지원자 공유, 실기기 알림·읽어주기·사진 선택, 오늘만 변경 알림은 아직 미구현 또는 미검증이다. 배포 안내와 앱 문서에서 로컬 시연 상태를 명시했다.

## 판정

**GitHub 업로드와 Vercel Preview 진행 가능.** 실제 주소가 생성된 뒤 모바일 설치와 응답 헤더를 한 번 더 확인해야 Production 출고가 완료된다.

## v1.2 추가 검토

- 레퍼런스의 정보 구조만 재해석하고 이름·로고·문구·그래픽 자산·배지·점수·소셜 기능은 복제하지 않았다.
- 오늘 화면의 주간 날짜와 시간대 그룹은 탐색을 돕지만, 날짜 선택이나 점수처럼 실제로 없는 기능을 암시하지 않는다.
- 할 일 편집·체크·복약 보조 행동·지원자 작성자 표시·되돌리기 동작을 유지했다.
- 모든 누르는 버튼은 일반 48px, 쉬운 모드 56px 최소 높이를 유지하고 상태를 색뿐 아니라 글자와 기호로 표시한다.
- React 구성·훅·접근성·렌더링 점검에서 남은 Critical/Important 항목은 0건이다.

## v1.3 T030 독립 검토

- 오늘 화면을 큰 개별 카드에서 시간대별 연결형 루틴 표로 바꾸고, 그룹 머리글에 완료 수를 배치한 구현이 설계 명세와 일치한다.
- 체크는 행 오른쪽의 독립 48px/56px 버튼이며 완료 상태를 색과 `✓` 기호, 접근성 상태로 함께 전달한다.
- 복약 보조 행동, 지원자 작성 표시, 사용자의 직접 편집 경로와 기록 화면 기능을 보존했다.
- 기록 격자와 관리 목록은 정보 밀도를 높였지만 성인용 문구, 읽기 쉬운 대비, 최소 터치 영역을 유지한다.
- 독립 검토 결과 Critical 0건 / Important 0건 / Minor 0건이다.

## v1.4 T031 독립 검토

- 주간 기록은 `할 일 + 월~일 7열`로 구현되어 날짜 숫자 없이 요일 패턴을 비교한다.
- 월간 기록은 할 일별 7열 달력 카드이며 월요일 기준 선행 공백, 날짜 숫자, 상태 기호, 완료 횟수를 제공한다.
- 기간 전환·이전/다음 콜백, 기록 훅 계약, 상태 화면, 체중 기록 이동을 보존했다.
- 상태 조회는 렌더마다 한 번 만든 `Map`을 사용하며 셀 조회는 반복 검색 없이 처리한다.
- 색상 하드코딩, `any`, 불필요한 선택 상태, 점수·퍼센트·순위·연속 기록 추가가 없다.
- 독립 검토와 390×844 재검증 결과 Critical 0건 / Important 0건 / Minor 0건이다.
