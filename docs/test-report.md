# 테스트 보고서

- 검증일: 2026-08-27
- 범위: 기존 앱 회귀 + T029 PWA·Vercel 배포 패키지
- 방식: 프로덕션 빌드, HTTP 산출물 검사, Puppeteer Chromium 390×844 실제 동선, 서비스 워커 오프라인 재실행

## 결과 요약

- 시나리오 7개 중 통과 7개
- 남은 Critical 0건 / Important 0건 / Minor 0건
- 테스트 중 발견·수정: ER-001, ER-002
- 실제 Vercel 주소와 실제 휴대폰 설치는 계정 연결 전이라 미검증

## 시나리오별 결과

| 시나리오 | 결과 | 실제 결과 |
|---|---|---|
| TypeScript 엄격 검사 | 통과 | 오류 0건 |
| Expo 공개 설정 해석 | 통과 | SDK 57, iOS·Android·web 구성 인식 |
| 웹 프로덕션 export | 통과 | `dist`와 471KB 해시 JS 번들 생성 |
| PWA 파일 완전성 | 통과 | manifest, 서비스 워커, 오프라인 HTML, CSS, 아이콘 6종 모두 `dist` 포함 |
| 처음 설정 모바일 화면 | 통과 | HTTP 200, 제목 `하루한칸`, 문서폭 390=viewport 390, 잘린 버튼 0 |
| 처음 설정→오늘 동선 | 통과 | 4회 조작 후 오늘 카드 표시, 콘솔·페이지·요청 오류 0 |
| 서비스 워커 오프라인 재실행 | 통과 | 등록·제어 확인 후 네트워크 차단 상태에서 오늘 화면 재표시 |

## 정적·보안 점검

- `public/sw.js` JavaScript 문법 검사 통과
- PWA 아이콘 실제 크기: 48, 180, 192, 512, 512(maskable), 1024px
- `package-lock.json`에 웹 필수 의존성 3종과 Node 22.x 고정 확인
- 코드에 실제 비밀키, `innerHTML`, `dangerouslySetInnerHTML`, `any`, `@ts-ignore`, 개발용 `console.log` 없음
- `.env`, `.vercel`, `node_modules`, `dist`, 작업 캐시는 Git 제외
- `npm audit`: 높음·치명적 0건. Expo 도구 경로의 중간 수준 항목은 호환 가능한 자동 수정안 없음

## 테스트 중 수정한 결함

1. Expo CLI가 샌드박스 밖 사용자 홈에 설정 폴더를 만들려던 환경 오류
2. 390px 화면에서 좌우 padding과 폭 100%가 겹쳐 오른쪽 버튼·카드가 잘리던 레이아웃 오류

## 아직 실행하지 못한 검증

- 실제 Vercel HTTPS 주소의 응답 헤더와 Preview/Production 자동 배포
- Android Chrome과 iPhone Safari에서의 홈 화면 설치
- Android·iOS 실기기의 알림·읽어주기·사진 선택
- 서로 다른 두 기기의 Supabase 원격 지원자 공유
- 실제 당사자 사용자의 도움 없는 장기 사용성 검사

## 사용자대표 한 줄 평

"390px에서도 큰 버튼과 오늘 카드가 잘리지 않고 바로 보이며, 점수 없이 차분하게 기록할 수 있다. 실제 사용자 검증과 기기 간 공유는 출시 전에 별도로 확인해야 한다."

## v1.2 시각 개편 회귀 검증

- 검증일: 2026-08-27
- TypeScript 엄격 검사와 Expo 웹 프로덕션 export 통과, 479KB 해시 JS 번들을 생성했다.
- Chrome 390×844에서 처음 설정→오늘→기록→오늘 동선을 자동 실행했다.
- `하루한칸`, 주간 날짜 띠, 시간대 루틴, 떠 있는 하단 메뉴, 주간 기록 범례가 모두 표시됐다.
- 오류 오버레이 0건, 콘솔 오류 0건, 페이지 오류 0건이었다.
- 프로덕션 정적 서버에서 서비스 워커 등록과 오프라인 새로고침을 다시 통과했다.
- React 점검 결과 불필요한 메모 의존성을 제거하고 기록 조회를 `Map`으로 바꿨다.

## v1.3 고밀도 루틴 보드 검증

- 검증일: 2026-08-28
- TypeScript 엄격 검사와 Expo 웹 프로덕션 export를 통과했고, 480KB 해시 JS 번들을 생성했다.
- Chrome 390×844에서 문서 폭 390px = viewport 390px를 확인했다.
- 주간 날짜 띠, 시간대별 연결형 루틴 패널, 시간·이모지·할 일·오른쪽 체크 배치가 표시됐다.
- `아침 약` 완료 → 체크 상태 표시 → 카드 내부 되돌리기를 실제 조작했고, 완료 배너 잔류 0건을 확인했다.
- 할 일 관리와 주간 기록 화면 이동, 서비스 워커 등록, 네트워크 차단 후 오프라인 재실행이 모두 통과했다.
- 오류 오버레이 0건, 콘솔 오류 0건, 페이지 오류 0건이었다.
- 테스트 중 발견한 ER-003은 공통 되돌리기 경로로 수정한 뒤 같은 시나리오로 재검증했다.

## v1.4 기록·통계 화면 검증

- 검증일: 2026-08-28
- TypeScript 엄격 검사, Expo 웹 프로덕션 export, diff 공백 검사를 통과했고 484KB 해시 JS 번들을 생성했다.
- Chrome 390×844에서 주간 표의 문서 폭·스크롤 폭·viewport가 모두 390px로 같았다.
- 주간 머리글은 날짜 숫자 없이 월~일 7개가 각각 한 번 표시됐고, 할 일 4개 × 7요일 = 28개 상태 칸을 확인했다.
- `아침 약`, `산책하기`, `양치하기`, `머리 감기` 이름이 모두 말줄임 없이 표시됐다.
- 이전 주 이동 시 `2026년 8월 · 5주`에서 `4주`로 바뀌고 다음 주로 원래 기간에 돌아왔다.
- 월간 전환 후 할 일별 달력 카드 4개와 날짜·상태 칸 124개가 표시됐으며, 문서 가로 넘침과 날짜·기호 겹침은 0건이었다.
- 체중 기록 버튼으로 체중 화면에 진입했고, 기록 화면 복귀 뒤 서비스 워커 등록과 네트워크 차단 상태의 오프라인 재실행을 통과했다.
- 콘솔 오류 0건, 페이지 오류 0건이었다.

## 사용자대표 한 줄 평 — v1.4

"주간은 날짜를 계산하지 않고 월요일부터 일요일까지 바로 비교할 수 있고, 월간은 할 일마다 실제 달력 모양으로 보여서 휴대폰에서도 패턴을 찾기 쉽다."
